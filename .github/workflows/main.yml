name: main 

on:
  push:
    branches:
      - dev

jobs:
  compile:
    name: basic compiling
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@2

      - name: compile code
        run: npx truffle compile
      
