name: main 

on:
  push:
    barnches:
      - dev

jobs;:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2

    - name: compile 
      run: npx truffle compile
      

